#+TITLE: Vault – Overview
#+DESCRIPTION: Zettelkasten knowledge management system.
#+FILETAGS: :VAULT:

* Overview
  Zettelkasten knowledge management system for atomic notes and interconnected ideas.

* Directory Layout
  | Path          | Purpose                                                          |
  |---------------+------------------------------------------------------------------|
  | =README.org=  | This file – high‑level description (you are reading it)           |
  | =docs/=       | Org/Markdown documents – notes, guides, references                |
  | =tasks/=      | Task files (`recurring.org`, `oneoff.org`, …)                    |
  | =lists/=      | Ordered collections (indexes, tags) – optional                  |
  | =assets/=     | Images, attachments, or any binary material – optional            |
  | =.logs/=      | Log file (`log.org`) – chronological change log                  |
  | =.git/=       | Git submodule (managed by the super‑repo)                         |

* Tag & State Conventions (inherited from the main repo)
  - *States* – TODO, PEND, GOING, DONE, ARCHIVED, etc. (see main README).
  - *Tags* – Use the hierarchical tag format:
    :VAULT:<CATEGORY>:<TYPE>:<DETAIL>:
    Example: =:VAULT:ZETTEL:CONCEPT:=

* Typical Files
  - =docs/<something>.org= – atomic zettels, evergreen notes, concept maps.
  - =tasks/recurring.org= – regular note‑review sessions, linking tasks.
  - =tasks/oneoff.org= – one‑time notes or knowledge‑capture actions.
  - =assets/…= – diagrams, mind maps, reference images.

* How to Use
  1. *Add a note* – create a new Org file under =docs/= and link to it from a task if needed.
  2. *Create a task* – add a headline under the appropriate file in =tasks/= using the standard TODO states.
  3. *Tag it* – follow the tag anatomy described above so Org‑mode search works across the whole super‑repo.
  4. *Commit* – daily commits are encouraged; the super‑repo pulls submodule updates automatically.

* References
  - Main repository README.org – overall philosophy, state & tag definitions:
    [[file:../README.org::#STATES and TAGS][Main README – STATES & TAGS]]
  - External tools: nvim‑orgmode, Orgzly‑android (see main README for integration tips).